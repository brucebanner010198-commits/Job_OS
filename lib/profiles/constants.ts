/** HTTP-only cookie storing the active profile id for server actions / RSC. */
export const PROFILE_COOKIE = "job_os_profile_id";

/** localStorage key mirrored on the client for instant UI feedback. */
export const PROFILE_STORAGE_KEY = "job_os_profile_id";

export const DEFAULT_PROFILE_NAME = "Default";
